<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The only colors that exist outside Compose: the window background the system paints before
      the first frame. Keeping it on-brand is what stops a white flash on cold start.
      The Compose palette lives in ui/theme/Color.kt.
    -->
    <color name="nm_window_background">#F5EAD8</color>
</resources>
