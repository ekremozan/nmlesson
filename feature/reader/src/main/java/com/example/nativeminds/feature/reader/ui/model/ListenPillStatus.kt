package com.example.nativeminds.feature.reader.ui.model

/** What [com.example.nativeminds.feature.reader.ui.components.ListenPill] draws — display-only. */
enum class ListenPillStatus {
    IDLE,
    PLAYING,
    PAUSED,
}
