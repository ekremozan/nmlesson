package com.example.nativeminds.feature.home.ui.model

/** Time-of-day bucket for the Home greeting — resolved to a string resource in the Composable. */
enum class GreetingPeriod {
    MORNING,
    AFTERNOON,
    EVENING,
}
